#include <./common/input.h>

// #include <cooperative_groups/scan.h>

int main(int argc, char *argv[]) {
  const int K = std::stoi(argv[2]);

  const input_t input = read_input(argv[1]);

  // compute histogram to count nodes degrees
  
}